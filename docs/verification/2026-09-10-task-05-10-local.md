# TASK-05/TASK-10 local verification — 2026-09-10

Environment: macOS arm64, Docker Desktop, Kind 0.33.0, Kubernetes 1.37.0,
Helm 4.2.4, kubectl 1.32.2.

## Results

| Check | Result |
|---|---|
| Ruff, formatting, strict mypy | passed |
| Unit/contract/ACL/SSE suite | 84 passed; JUnit and coverage XML generated |
| Disposable integration and generated-course E2E | 16 passed |
| RAG gate | Recall@5/MRR/citation precision/abstention F1 = 1.0; p95 = 1.28 ms |
| React test/build | 2 passed; production build succeeded |
| Frozen Buildx image graph/build | five images built successfully |
| Helm lint/template determinism/package | passed; `course-tutor-0.1.0.tgz` generated |
| Kind Helm install/upgrade | release revision 4 deployed |
| Helm test | succeeded |
| Backend topology | one Ready Pod, regular containers `agent-api` and `practice-api` |
| Runtime identity | all application and dev-dependency containers use non-zero UIDs |

## TASK-06/TASK-10 follow-up verification

| Check | Result |
|---|---|
| Final static/quick regression | Ruff/format/mypy passed; 91 Python tests passed |
| Memory/teaching unit and policy suites | passed, including Chinese/English extraction, hard budgets, decay and solution gating |
| Disposable integration/E2E suite | 22 passed; opt-in, scope, dedup, conflict, correction, export, tombstone and physical purge covered |
| Migration round trip/schema parity | upgrade/downgrade/re-upgrade passed at `a41c9e7d2b60`; no model drift |
| Frontend | 2 Vitest tests passed; TypeScript/Vite production build succeeded |
| Buildx | Agent, Practice, worker, Web and fake-LLM `:ci` images rebuilt successfully |
| Expanded Helm smoke | generated fixture ingested/embedded/published/listed; SSE returned ordered token, citation and done events |
| Backend topology | one Pod contains exactly `agent-api` and `practice-api`; both Ready |
| Controlled negatives | invalid Helm replica count and isolated broken-readiness rollout both failed; temporary Deployment was removed |

The final verified Helm release is revision 14 with status `deployed`; it remains
running in namespace `course-tutor` on context `kind-course-tutor-local`. The
synthetic fixture contains no real course material.

The deployment remains running in namespace `course-tutor` on context
`kind-course-tutor-local`. The completed migration Job is expected to show 0/1 Ready
because successful Job containers terminate.

GitHub-hosted execution and the configured Trivy/Gitleaks/SBOM jobs require a commit
and push. No remote mutation was performed during this local verification.
