# TASK-17 persistent K-12 storage verification — 2026-09-16

The implementation was validated on macOS arm64 with disposable Docker dependencies.
No NAS credentials, textbook contents or production secrets are recorded here.

## Capacity and placement

- The measured ChinaTextbook checkout is approximately 85 GiB, including roughly
  42 GiB of Git history and 43 GiB of working-tree content.
- The working tree contains 1,905 complete PDFs (29.7 GiB) plus 466 split parts for
  182 additional books (13.7 GiB). A 47-file page sample projects about 180,000 pages.
- The documented topology keeps source textbooks and backup artifacts on NAS,
  PostgreSQL on durable transactional storage, and live Qdrant segments on SSD/block
  storage. Qdrant snapshots use a separate NAS-backed claim.
- Local real-content values request 2 GiB and limit Qdrant to 4 GiB. Source artifact
  duplication is disabled for immutable NAS originals.

## Functional evidence

| Check | Result |
|---|---|
| PostgreSQL migration upgrade and Alembic model drift check | passed |
| Unit tests | 97 passed |
| Disposable integration/E2E regression | passed |
| Unchanged-file cross-version source/chunk reuse | passed |
| Source-membership-scoped retrieval | passed |
| Automatic scan opt-out with manual ingestion retained | passed |
| SHA-256 object reuse across renamed files | passed |
| MinIO-disabled worker configuration | passed |
| Ruff and formatting | passed |
| Helm lint and local-real render | passed |
| Requirement/contract validator | 41 requirements, 17 tasks and 28 operations valid |
| Delivery validator | passed |

## Helm render evidence

The real-content profile rendered independent PostgreSQL and Qdrant data claims, a
separate existing Qdrant snapshot claim, and the snapshot CronJob. The Qdrant workload
rendered with a 4 GiB memory limit and `STORE_SOURCE_ARTIFACTS=false`. Disposable CI
values continue to use non-persistent internal dependencies.

No production installation was performed. The snapshot claim must be provisioned by
the target cluster/NAS integration before enabling the CronJob in that environment.
